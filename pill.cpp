#include <iostream>
#include <vector>
using namespace std;

// main() is where program execution begins.
int main() {

    int n_moleculas, n_ligacoes;
    cin >> n_moleculas >> n_ligacoes;

    // cria a estrutura do grafo
 
 
    // loop para ler as arestas do grafo
    for (int i = 0; i < n_ligacoes; ++i) {
        
        // le os vertices i e j
        int node_i, node_j;
        cin >> node_i >> node_j;

        // adiciona uma aresta entre eles
 
    }

    return 0;
}
